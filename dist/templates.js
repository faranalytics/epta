import { $ } from 'elemental-0';
export let template = $('!DOCTYPE html')($('html', { 'lang': 'en' })($('head')($('style')(`
                main#http-response {
                    display:flex;
                    flex-direction: column;
                    justify-content: center;
                }
                `)), $('body')($('main', { 'id': 'http-response' })()), $('footer')()));
